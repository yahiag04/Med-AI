version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_SECRET_KEY=dev-change-me
    volumes:
      - ./app.db:/app/app.db
      - ./uploads:/app/uploads
